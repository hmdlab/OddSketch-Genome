#!/usr/bin/env bash
set -euo pipefail

# This project primarily uses synthetic genomes generated by scripts.
# No mandatory external dataset download is required.

echo "[download_data] No external dataset is required for the default benchmark."
echo "[download_data] Synthetic genomes are generated during run_benchmark.sh."

echo "[download_data] If you use custom datasets, place them under:"
echo "  experiments/outputs/custom_data"
